Screenshots referenced by the project README.

Generated with `./scripts/capture-screenshots.sh`, which names them, puts them
here and halves retina captures so the README stays quick to load.

Check every image for anything private before committing. A transcript in a
screenshot is public permanently, and an email address or a client name in one
cannot be taken back.
